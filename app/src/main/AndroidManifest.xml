<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cr1ss.androidmod2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidMod2">
        <activity
            android:name=".ui.dbraw.DBRawActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.corrutinas.CoroutinesActivity"
            android:exported="true">

        </activity>
        <activity
            android:name=".ui.navigationcomponent.NavCompActivity"
            android:exported="true"></activity>
        <activity
            android:name=".ui.fragmentos.FragmentosActivity"
            android:exported="true" />
        <activity
            android:name=".ui.home.MainActivity"
            android:exported="true" />
    </application>

</manifest>